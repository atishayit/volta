# Screenshots

Captured from the live static export (real PJM data, PJM East).

- `hero.png` — header, headline metrics, the actual-vs-predicted forecast chart, and
  the in-browser what-if simulator.
- `dashboard-full.png` — the full dashboard: forecast, what-if, model scorecard with
  the per-horizon crossover, and the anomaly timeline.

For a 15–20s screen recording of the what-if simulator in action, record the live
demo once deployed (move the temperature / day-type / hour controls and watch the
forecast curve re-compute in-browser).
